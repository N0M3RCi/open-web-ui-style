# Production deployment — self-hosted (local proxy mode)
VITE_USE_LOCAL_PROXY=true
VITE_BASE_URL=/api
VITE_BRAIN_ENDPOINT=/brain/api
# Absolute URL for the brain to reach the API server (for step sync, overlay, etc.)
VITE_SERVER_URL=http://localhost:3001
